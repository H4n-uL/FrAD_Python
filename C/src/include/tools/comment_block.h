#ifndef COMMENT_BLOCK_H
#define COMMENT_BLOCK_H

#include <stdio.h>

unsigned char* comment(char* title, unsigned char* data, int data_length);
unsigned char* image(unsigned char* data, int data_length);

#endif /* COMMENT_BLOCK_H */
